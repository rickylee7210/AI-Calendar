import 'package:flutter/material.dart';

/// App-wide theme constants matching HyperOS / MiSans design system.
class AppTheme {
  AppTheme._();

  static const String fontFamily = 'MiSans';
  static const Color bgColor = Color(0xFFF8FBFF);
  static const Color selectedRed = Color(0xCCFA382E); // 80% opacity
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Color(0x99000000); // 60% opacity
}
